<header class="navbar navbar-light bg-light navbar-expand shadow">
  <div class="container d-flex justify-content-between align-items-center">
    <!-- ロゴ -->
    <a href="/" class="fs-1 fw-bold navbar-brand">HDeats</a>

    <!-- ナビゲーションメニュー -->
    <nav>
      <ul class="nav d-flex align-items-center flex-nowrap">
        <% if logged_in? %>
          <li class="nav-item">
            <li class="nav-item">
              <a href="/" class="fs-3 nav-link  fw-bold text-reset">記録</a>
            </li>
            <a href="<%= users_path %>" class="fs-3 nav-link text-reset fw-bold">みんなの食事</a>
            <li class="nav-item dropdown">
            <button class="fs-3 nav-link fw-bold text-reset dropdown-toggle" id="navbarDropdown" data-bs-toggle="dropdown" aria-expanded="false">
              アカウント
            </button>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li class="dropdown-item"><a href="<%= user_path(current_user) %>" class="nav-link text-reset fw-bold">プロフィール</a></li>
              <li class="dropdown-item"><a href="<%= edit_user_path(current_user) %>" class="nav-link text-reset fw-bold">設定</a></li>
              <li><hr class="dropdown-divider"></li>
              <li class="dropdown-item"><%= link_to "ログアウト", logout_path, method: :delete, data: { turbo_method: :delete }, class: "nav-link text-reset fw-bold" %></li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <a href="/" class="fs-3 nav-link  fw-bold text-reset">ホーム</a>
          </li>
          <li class="nav-item">
            <a href="<%= login_path %>" class="fs-3 nav-link text-reset fw-bold">ログイン</a>
          </li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
